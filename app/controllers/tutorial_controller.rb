class TutorialController < ApplicationController
	def babyteeth
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_intro
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_form_1
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_form_2
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_form_3
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_form_4
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_prev_1
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_prev_2
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

	def cav_prev_3
		@user = current_user
		if @user
			@profile = @user.profile
		end
	end

end